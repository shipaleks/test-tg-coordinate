# 🔍 Почему бот сломался без изменений в коде?

## 📅 Временная линия

- **9 сентября 2025**: Последний коммит в репозиторий
- **Сентябрь - октябрь**: Бот работал нормально ✅
- **Начало ноября 2025**: Бот начал падать с ошибкой ❌
- **15 ноября 2025**: Обнаружена и исправлена проблема

## 🐛 Что произошло

### Основная причина: Koyeb изменил политику healthcheck

**До изменений:**
- Koyeb был более "снисходительным" к приложениям без healthcheck
- Instance мог работать неопределённо долго
- Отсутствие healthcheck не приводило к остановке

**После изменений:**
- Koyeb начал агрессивнее проверять healthcheck endpoints
- Если приложение не отвечает на `/` или `/health` → instance останавливается
- Timeout стал короче
- В логах появилось: `Instance is stopping.`

**Почему это повлияло на ваш бот:**
- Бот работал в webhook режиме
- `python-telegram-bot` создаёт HTTP сервер только для Telegram обновлений
- Но НЕ создаёт endpoint для healthcheck
- Koyeb пытался проверить `/` → получал 404 → останавливал instance

### Вторичная причина: Нефиксированные версии зависимостей

**Проблема в requirements.txt:**
```python
# ДО (плохо)
openai>=1.54.0  # Любая версия >= 1.54.0
aiohttp>=3.8.0
python-dotenv>=1.0.0

# ПОСЛЕ (хорошо)
openai==1.54.4  # Точная версия
aiohttp==3.10.11
python-dotenv==1.0.1
```

**Что могло пойти не так:**
- При каждом деплое Koyeb устанавливал **последнюю** версию пакетов
- OpenAI SDK обновляется часто (1.54 → 1.55 → 1.56...)
- Изменения в API могли сломать генерацию фактов
- Но это не объясняет полное падение instance

## ✅ Что было исправлено

### 1. Добавлен healthcheck endpoint

**Файл**: `src/main.py`

```python
# Добавлены endpoints:
GET /       → 200 OK
GET /health → 200 OK  
GET /healthz → 200 OK
```

Теперь Koyeb может проверить что приложение живое.

### 2. Зафиксированы версии зависимостей

**Файл**: `requirements.txt`

Все версии теперь точные (`==` вместо `>=`):
- Стабильность: одинаковые версии на каждом деплое
- Предсказуемость: нет сюрпризов от обновлений
- Отладка: легче воспроизвести проблемы локально

### 3. Добавлен .python-version

Фиксирует версию Python для разных окружений.

## 🔮 Почему это не было проблемой раньше?

### Возможные сценарии:

**Сценарий 1: Постепенное ужесточение**
```
Август: Koyeb проверяет healthcheck каждые 5 минут (щадящий режим)
    ↓
Сентябрь: Koyeb проверяет каждые 2 минуты
    ↓
Октябрь: Koyeb проверяет каждую минуту
    ↓
Ноябрь: Koyeb останавливает instance после 3 неудачных проверок
```

**Сценарий 2: Обновление платформы**
```
Koyeb обновил свою инфраструктуру
    ↓
Новые дефолтные настройки для healthcheck
    ↓
Старые приложения (без healthcheck) начали падать
```

**Сценарий 3: Free tier изменения**
```
Раньше: Free tier instances работали 24/7
    ↓
Изменение политики: Free tier instances останавливаются если неактивны
    ↓
Без healthcheck Koyeb не может определить активность
```

## 🛡️ Как избежать таких проблем в будущем

### 1. Всегда фиксируйте версии

```python
# ❌ Плохо
package>=1.0.0

# ✅ Хорошо
package==1.2.3
```

### 2. Всегда добавляйте healthcheck

Для любого HTTP сервиса в продакшене:
```python
@app.get("/health")
def health():
    return {"status": "ok"}
```

### 3. Мониторьте uptime

Используйте внешние сервисы:
- UptimeRobot (бесплатный)
- Pingdom
- StatusCake

Они будут проверять ваш бот и уведомлять о падениях.

### 4. Читайте changelogs платформы

Подпишитесь на:
- Koyeb status page: https://status.koyeb.com/
- Koyeb changelog/blog
- Email уведомления от Koyeb

### 5. Регулярное тестирование

Даже если код не меняется:
- Тестируйте бота раз в неделю
- Проверяйте логи на warning/error
- Следите за метриками (response time, uptime)

## 📊 Сравнение: До vs После

| Аспект | До | После |
|--------|-----|--------|
| **Healthcheck** | ❌ Нет | ✅ Есть (/, /health, /healthz) |
| **Версии зависимостей** | ⚠️ Нефиксированные (`>=`) | ✅ Зафиксированные (`==`) |
| **Python версия** | ⚠️ Не указана | ✅ Зафиксирована (3.12) |
| **Uptime** | ❌ Падает через ~10 минут | ✅ Работает стабильно |
| **Предсказуемость** | ⚠️ Низкая | ✅ Высокая |

## 🎯 Выводы

1. **Облачные платформы меняются** без вашего ведома
2. **Best practices существуют не просто так** - healthcheck должен быть всегда
3. **Фиксирование версий** критично для продакшена
4. **Мониторинг** помогает обнаружить проблемы раньше пользователей
5. **Документация** (этот файл) помогает понять что произошло

## 🚀 Что делать дальше

1. ✅ Задеплоить исправления (healthcheck + фиксированные версии)
2. ✅ Настроить healthcheck в Koyeb UI
3. ✅ Протестировать бота
4. ⏭️ Настроить мониторинг (UptimeRobot)
5. ⏭️ Подписаться на Koyeb status updates

---

**Мораль**: Даже если ваш код идеален, окружение может измениться. Всегда следуйте best practices для production deployments.

