# 🎯 Новая стратегия моделей и reasoning

## Дата: 19 ноября 2025

---

## 📊 ЧТО ИЗМЕНИЛОСЬ

### ⚡ Новые дефолты (для всех пользователей):

**Модель**: `gpt-5.1` (full model)  
**Reasoning**: `none` (быстро, 10-15s)  
**web_search**: ✅ Работает!

**Результат**:
- Быстрые факты для всех (10-15s vs 30-60s)
- Всё равно с проверкой через web_search
- Отличное качество при минимальной задержке

---

## 🎁 Донаторы получают бонус (автоматически)

### Скрытая награда за поддержку:

При донате **автоматически** (без уведомления):
- Reasoning: `none` → `low` (улучшение)
- Модель: остаётся `gpt-5.1`

**Что это даёт**:
- Более глубокие факты (15-25s vs 10-15s)
- Больше web_search проверок
- Лучше детали и имена
- Тщательнее верификация

**Сообщение донатору**:
```
🎉 Спасибо за поддержку!
🧠 Факты теперь будут генерироваться с улучшенным reasoning
   (больше проверок и деталей).
```

Деликатно, не агрессивно.

---

## 🛠️ Ручные настройки (скрытая команда)

### `/reason` команда (доступна всем):

```
Developer settings:

⚪ Reasoning: none     ← Дефолт для всех
✅ Reasoning: low      ← Автоматически для донаторов
⚪ Reasoning: medium
⚪ Reasoning: high

✅ Model: gpt-5.1      ← Дефолт для всех
⚪ Model: gpt-5.1-mini ← (показан, но маппится на gpt-5.1)
```

**Использование**:
- Команда скрытая (не в меню)
- Для экспериментов и отладки
- Обычные пользователи не знают о ней

---

## 🔄 Автоматический маппинг

### Модели (все → gpt-5.1):

```python
MODEL_MAPPING = {
    "gpt-5": "gpt-5.1",           # Legacy
    "gpt-5-mini": "gpt-5.1",      # Mini has access issues
    "gpt-5.1-mini": "gpt-5.1",    # Mini has access issues
}
```

**Результат**: Все используют `gpt-5.1` независимо от того что в БД!

### Reasoning (legacy support):

```python
REASONING_MAPPING = {
    "minimal": "low",  # Legacy minimal → low
    "none": "none",
    "low": "low",
    "medium": "medium",
    "high": "high",
}
```

**+ Donor bonus**:
```python
if mapped_level == "none" and is_donor:
    return "low"  # Auto-upgrade!
```

---

## 📈 Производительность

### Для обычных пользователей (reasoning=none):

**Static location**:
- Запрос: ~10-15 секунд
- web_search: 1-2 запроса
- Качество: Хорошее ✅

**Live location** (интервал 5 мин):
- Факт: ~10-15 секунд
- За час: 12 фактов легко
- web_search: всё проверяется ✅

### Для донаторов (reasoning=low, автоматически):

**Static location**:
- Запрос: ~15-25 секунд (+50% времени)
- web_search: 2-3 запроса
- Качество: Отличное ✅✅

**Live location** (интервал 5 мин):
- Факт: ~15-25 секунд
- За час: 12 фактов без проблем
- web_search: тщательная проверка ✅✅

---

## 💰 Стоимость

### Обычные пользователи (none):

**Стоимость на факт**: ~$0.003-0.005 (0.3-0.5¢)
- Input: ~2000 tokens
- Output: ~400 tokens
- Reasoning: 0 tokens (none)

**$10 хватит на**: ~2000-3000 фактов

### Донаторы (low):

**Стоимость на факт**: ~$0.005-0.008 (0.5-0.8¢)
- Input: ~2000 tokens
- Output: ~500 tokens
- Reasoning: ~200 tokens (low)

**$10 хватит на**: ~1200-2000 фактов

**Разница**: +60% стоимость, но +100% качество! 🎯

---

## 🎁 Почему это хорошая стратегия

### Для обычных пользователей:

✅ **Быстро** - никто не ждёт 30+ секунд  
✅ **Качественно** - web_search всё проверяет  
✅ **Доступно** - дешевле = больше фактов на $10  

### Для донаторов:

✅ **Награда** - автоматически лучшие факты  
✅ **Скрыто** - не агрессивный upsell  
✅ **Ценно** - заметная разница в качестве  

### Для проекта:

✅ **Экономия** - большинство используют none (дешевле)  
✅ **Retention** - донаторы видят ценность  
✅ **Scalable** - 1000+ пользователей без больших затрат  

---

## 📊 Сравнение: До vs После

| Параметр | До (v1.3.0) | После (v1.3.1) |
|----------|------------|----------------|
| **Дефолтная модель** | gpt-5-mini | gpt-5.1 (full) |
| **Дефолт reasoning** | medium | none |
| **Скорость** | 30-60s | 10-15s ⚡ |
| **Качество** | Отличное | Хорошее (web_search!) |
| **Донатор скорость** | 30-60s | 15-25s |
| **Донатор качество** | Отличное | Отличное+ |
| **Стоимость/факт** | $0.01 | $0.004 (обычные), $0.007 (донаторы) |

---

## 🧪 Тестирование после деплоя

### Проверка 1: Обычный пользователь

1. Создайте тестового пользователя (не донатор)
2. Отправьте локацию
3. **Проверьте логи**:
   ```
   ✅ GPT-5.1 Responses: sending request (model=gpt-5.1, reasoning=none, ...)
   ✅ GPT-5.1 Responses: success (web_search enabled)
   ```
4. **Засеките время**: 10-15 секунд ⚡

### Проверка 2: Донатор

1. Используйте аккаунт с донатом
2. Отправьте локацию
3. **Проверьте логи**:
   ```
   ✅ GPT-5.1 Responses: sending request (model=gpt-5.1, reasoning=low, ...)
   ✅ GPT-5.1 Responses: success (web_search enabled)
   ```
4. **Засеките время**: 15-25 секунд
5. **Проверьте качество**: должно быть заметно глубже

### Проверка 3: Ручное изменение

1. Отправьте `/reason`
2. Попробуйте каждый режим: none, low, medium, high
3. Сравните скорость и качество

---

## ✅ Миграция существующих пользователей

### Автоматическая (через маппинг в коде):

**В БД может быть**:
- `model: "gpt-5-mini"` → Код использует `"gpt-5.1"`
- `reasoning: "medium"` → Код использует `"medium"` (уважает выбор)

**Новые пользователи**:
- `model: "gpt-5.1"` (из DEFAULT в SQL)
- `reasoning: "none"` (из DEFAULT в SQL)

**Донаторы** (автоматически):
- Если `reasoning: "none"` → Код возвращает `"low"` ✅
- Если `reasoning: "medium"` → Остаётся `"medium"` (уважает ручной выбор)

---

## 🎯 Итоговая конфигурация

### Все пользователи (дефолт):

```python
model = "gpt-5.1"
reasoning = "none"
web_search = enabled
latency = 10-15s
quality = Good ✅
cost = $0.004/fact
```

### Донаторы (автоматический бонус):

```python
model = "gpt-5.1"
reasoning = "low"  ← AUTO-UPGRADE!
web_search = enabled
latency = 15-25s
quality = Excellent ✅✅
cost = $0.007/fact
```

### Ручная настройка (через /reason):

```python
model = "gpt-5.1"
reasoning = user_choice  # none/low/medium/high
web_search = enabled
latency = 10-120s (depends on reasoning)
quality = Good → Perfect
```

---

## 🚀 ГОТОВО К ДЕПЛОЮ!

**После пересборки Koyeb** (~5 минут):

- ✅ Все пользователи на gpt-5.1 + reasoning=none
- ✅ Быстрые факты (10-15s)
- ✅ web_search работает
- ✅ Донаторы автоматически на reasoning=low
- ✅ /reason доступен для экспериментов

**Результат**: Быстрый, качественный, экономичный бот! ⚡💰🎯

---

**Версия**: v1.3.1  
**Статус**: 🟢 Ready to deploy  
**Next check**: Через 10 минут после деплоя

