# ✅ Правильное использование GPT-5.1

## 📚 Официальная документация

- [OpenAI Latest Model Guide](https://platform.openai.com/docs/guides/latest-model)
- [GPT-5.1 Prompting Guide](https://cookbook.openai.com/examples/gpt-5/gpt-5-1_prompting_guide)

---

## 🎯 Актуальные модели GPT-5.1

### Доступные модели:

| Модель | Назначение | Reasoning | Скорость |
|--------|-----------|-----------|----------|
| **gpt-5.1** | Flagship, максимальное качество | low/medium/high | Медленная |
| **gpt-5.1-mini** | Быстрая, экономичная | low/medium/high | Средняя |
| **gpt-5.1-codex** | Специально для кода | low/medium/high | Средняя |

### Reasoning Modes:

| Mode | Когда использовать | Tokens | Latency |
|------|-------------------|---------|---------|
| **none** | Простые задачи без reasoning | Минимум | <1s |
| **low** | Базовый reasoning | Мало | 2-5s |
| **medium** | Сбалансированный (дефолт) | Средне | 5-15s |
| **high** | Сложные задачи | Много | 15-60s |

---

## 🔧 Текущие проблемы в коде

### ❌ Проблема 1: Неправильное использование Responses API

**Текущий код**:
```python
response = await self.client.responses.create(
    model=user_model,  # "gpt-5" или "gpt-5.1"
    input=messages,
    tools=tools,
    tool_choice="auto",
    reasoning={"effort": "medium"},
)
```

**Проблема**: SDK v1.58.1 не поддерживает `responses` API

**Решение**: Обновлено до **openai==1.99.2** ✅

### ❌ Проблема 2: Пользователи с устаревшими model names

**В БД хранится**: "gpt-5" и "gpt-5-mini"  
**Должно быть**: "gpt-5.1" и "gpt-5.1-mini"

**Решение**: Запустить миграцию:
```bash
python -m src.utils.migrate_model_names
```

### ❌ Проблема 3: `<answer>` парсинг ломается

**Проблема**: o4-mini не всегда закрывает `</answer>` тег

**Было**:
```python
answer_match = re.search(r"<answer>(.*?)</answer>", response, re.DOTALL)
```

**Стало** ✅:
```python
answer_match = re.search(r"<answer>(.*?)(?:</answer>|$)", response, re.DOTALL)
```

### ❌ Проблема 4: Live session останавливается рано

**Было**: Timeout 3 минуты → сессия завершается после 3 фактов  
**Стало** ✅: Timeout 10 минут + обновление `last_update` после каждого факта

---

## 📝 Правильная конфигурация для Bot Voyage

### Для Live Location (GPT-5.1-mini, medium reasoning)

```python
# Используется для генерации фактов во время прогулки
model = "gpt-5.1-mini"
reasoning = {"effort": "medium"}  # Баланс качества и скорости
tools = [{"type": "web_search"}]
tool_choice = "auto"
```

**Параметры**:
- **model**: `gpt-5.1-mini` (быстрее, дешевле)
- **reasoning.effort**: `medium` (хорошее качество за разумное время)
- **web_search**: Обязательный для проверки фактов
- **Latency**: ~15-40 секунд

### Для Static Location (GPT-4.1 fallback)

```python
# Fallback если Responses API недоступен
model = "gpt-4.1"
max_tokens = 400
temperature = 0.7
```

**Параметры**:
- **model**: `gpt-4.1` (быстрая генерация)
- **Latency**: ~3-8 секунд

---

## 🔄 Миграция существующих пользователей

### Автоматическая миграция БД

**Запустите на production**:

```bash
# Для Koyeb/Railway (через SSH или Run command)
python -m src.utils.migrate_model_names
```

**Что делает скрипт**:
- Находит всех пользователей с `model = "gpt-5-mini"`
- Обновляет на `"gpt-5.1-mini"`
- Находит всех с `model = "gpt-5"`
- Обновляет на `"gpt-5.1"`
- Показывает статистику

### Ручная миграция (если нужно)

#### PostgreSQL:
```sql
-- Обновить на gpt-5.1-mini
UPDATE user_preferences 
SET model = 'gpt-5.1-mini', updated_at = CURRENT_TIMESTAMP
WHERE model = 'gpt-5-mini';

-- Обновить на gpt-5.1
UPDATE user_preferences 
SET model = 'gpt-5.1', updated_at = CURRENT_TIMESTAMP
WHERE model = 'gpt-5';

-- Проверить результат
SELECT model, COUNT(*) FROM user_preferences GROUP BY model;
```

#### Firestore:
```javascript
// В Firebase Console или через скрипт
const users = await db.collection('users')
  .where('model', '==', 'gpt-5-mini')
  .get();

users.forEach(async (doc) => {
  await doc.ref.update({ 
    model: 'gpt-5.1-mini',
    updated_at: Date.now() 
  });
});
```

---

## 🧪 Тестирование после обновления

### Проверка 1: SDK версия

В логах должно быть:
```
✅ GPT-5.1 Responses: sending request (model=gpt-5.1-mini, reasoning=medium, tool_choice=auto)
✅ GPT-5.1 Responses: success (web_search enabled)
```

**НЕ должно быть**:
```
❌ 'AsyncOpenAI' object has no attribute 'responses'
❌ Falling back to standard models
```

### Проверка 2: Парсинг ответа

Пользователю должно приходить:
```
🔴 Факт #1

📍 Место: Улица Фоссе-Сен-Жак, Париж

💡 Факт: В глубине двора под одной из оконных арок...

🔗 Источники:
- Vestiges du mur... — [URL]
```

**НЕ должно быть**:
```
❌ 💡 Факт: <answer>
   Location: ...
   Interesting fact: ...
```

### Проверка 3: Нумерация

**Должно быть**:
- Факт #1 (первый факт)
- Факт #2 (второй факт)
- Факт #3 (третий факт)

**НЕ должно быть**:
- Факт #4 (первый факт) ❌
- Факт #7 (второй факт) ❌

### Проверка 4: Длительность сессии

**Тест**: Запустить live location на 1 час с интервалом 5 минут

**Ожидается**: 
- ~12 фактов за час (60 мин / 5 мин = 12)
- Сессия работает всё время
- Завершается через 60 минут

**НЕ должно**:
- Останавливаться после 3 фактов ❌
- Завершаться раньше времени ❌

---

## 📊 Оптимальные настройки reasoning

Согласно [документации GPT-5.1](https://cookbook.openai.com/examples/gpt-5/gpt-5-1_prompting_guide):

### Для Bot Voyage рекомендую:

**Live Location** (качество важнее скорости):
```python
model = "gpt-5.1-mini"
reasoning = {"effort": "medium"}  # Баланс
```

**Static Location** (скорость важна):
```python
model = "gpt-5.1-mini"
reasoning = {"effort": "low"}  # Быстрее
```

**Донаторы** (максимальное качество):
```python
model = "gpt-5.1"  # Полная модель
reasoning = {"effort": "high"}  # Глубокий reasoning
```

---

## 🔍 Debugging: Как проверить что используется

### В логах ищите:

**Правильно** ✅:
```
GPT-5.1 Responses: sending request (model=gpt-5.1-mini, reasoning=medium, tool_choice=auto)
```

**Неправильно** ❌:
```
GPT-5.1 Responses: sending request (model=gpt-5, ...)
                                           ^^^^^^ старое название!
```

### Команда для проверки модели пользователя:

```python
# В боте добавьте /mymodel команду
@handler
async def mymodel_command(update, context):
    donors_db = await get_async_donors_db()
    model = await donors_db.get_user_model(update.effective_user.id)
    reasoning = await donors_db.get_user_reasoning(update.effective_user.id)
    
    await update.message.reply_text(
        f"🤖 Your settings:\n"
        f"Model: {model}\n"
        f"Reasoning: {reasoning}"
    )
```

---

## ✅ Чек-лист обновления

### Код:
- [x] OpenAI SDK → 1.99.2
- [x] Удалён httpx pin (не нужен)
- [x] Парсинг <answer> гибкий
- [x] Нумерация фактов исправлена
- [x] Timeout увеличен до 10 минут
- [x] last_update обновляется после фактов

### База данных:
- [ ] Миграция model names (запустить скрипт)
- [ ] Проверить что дефолты правильные

### Тестирование:
- [ ] Проверить что SDK 1.99.2 задеплоилась
- [ ] Проверить парсинг (нет сырого <answer>)
- [ ] Проверить нумерацию (#1, #2, #3...)
- [ ] Проверить длительность сессии (час = ~12 фактов)
- [ ] Проверить что используется gpt-5.1-mini (не gpt-5)

---

## 🚀 Действия сейчас

### 1. Дождаться пересборки Koyeb (~5 минут)

Koyeb пересобирается с:
- ✅ openai==1.99.2 (поддержка Responses API)
- ✅ Исправленный парсинг
- ✅ Исправленная нумерация
- ✅ Исправленный timeout

### 2. Запустить миграцию БД

**Для Firestore** (ваш случай):

Можно сделать через Firebase Console или добавить в код временный хук:

```python
# В main.py после инициализации, один раз
if os.environ.get("RUN_MODEL_MIGRATION", "").lower() == "true":
    import asyncio
    from src.utils.migrate_model_names import migrate_model_names
    asyncio.run(migrate_model_names())
```

Затем в Koyeb:
- Добавить `RUN_MODEL_MIGRATION=true`
- Redeploy
- После миграции удалить переменную

### 3. Протестировать

После пересборки (~10 минут):
1. Запустить новую live location сессию (1 час, интервал 5 мин)
2. Проверить что:
   - ✅ Факты парсятся правильно (нет <answer>)
   - ✅ Нумерация последовательная (#1, #2, #3...)
   - ✅ Сессия работает всю длительность
   - ✅ В логах "GPT-5.1 Responses: success"

---

**Следующий коммит**: запушу миграцию и финальные исправления!

