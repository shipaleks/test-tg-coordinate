# 🔄 Инструкция: Миграция моделей gpt-5 → gpt-5.1

## 🎯 Цель

Обновить всех существующих пользователей с:
- `gpt-5` → `gpt-5.1`
- `gpt-5-mini` → `gpt-5.1-mini`

---

## 📋 Шаг 1: Добавить переменную окружения в Koyeb

### Откройте Koyeb Dashboard

1. Перейдите в **Service** → **Settings** → **Environment Variables**
2. Нажмите **Add Variable**
3. Добавьте:

```
Name: RUN_MODEL_MIGRATION
Value: true
```

4. **Сохраните** (Save)

⚠️ **НЕ нажимайте Deploy yet!** Сначала подождите текущего деплоя.

---

## 📋 Шаг 2: Дождаться текущей пересборки

Koyeb сейчас пересобирается с:
- `openai==1.99.2`
- Исправленный парсинг
- Исправленная нумерация
- Исправленный timeout

**Проверьте в Deployments**:
- Должен появиться новый deployment (от последнего коммита `cb7aa0c`)
- Статус: **Building** → **Deploying** → **Healthy**

**Подождите**: ~5-10 минут до статуса **Healthy**

---

## 📋 Шаг 3: Запустить миграцию

### После того как deployment стал Healthy:

1. В Koyeb нажмите **Redeploy** (или он сам задеплоится через минуту после добавления переменной)
2. Откройте **Runtime Logs**
3. Следите за логами

### В логах должно появиться:

```
✅ Starting Bot Voyage...
✅ Firestore detected...
✅ Model names migration completed: gpt-5 → gpt-5.1, gpt-5-mini → gpt-5.1-mini
✅ Updated user 5857876: gpt-5 → gpt-5.1
✅ Total users updated: 1 (или больше)
✅ Current model distribution:
     gpt-5.1: 1 users
     gpt-5.1-mini: 0 users
✅ Application started
```

**Если появится**:
```
⚠️ Model migration failed or skipped: [error]
```
→ Скопируйте полную ошибку, я помогу исправить

---

## 📋 Шаг 4: Удалить переменную окружения

**КРИТИЧНО**: После успешной миграции удалите переменную!

1. **Koyeb** → **Service** → **Settings** → **Environment Variables**
2. Найдите `RUN_MODEL_MIGRATION`
3. Нажмите **Delete** (крестик)
4. **Save**

⚠️ **Почему нужно удалить**:
- Миграция запускается при каждом старте
- После первого раза она не нужна
- Может замедлить старт приложения

---

## 📋 Шаг 5: Проверить результат

### Способ 1: Проверить через бота

Отправьте боту новую геолокацию и проверьте логи:

**До миграции**:
```
GPT-5.1 Responses: sending request (model=gpt-5, reasoning=low, ...)
                                           ^^^^^^ старое название
```

**После миграции**:
```
GPT-5.1 Responses: sending request (model=gpt-5.1, reasoning=low, ...)
                                           ^^^^^^^^ новое название!
```

### Способ 2: Проверить через Firebase Console

**Firebase Console** → **Firestore Database** → **users** → **5857876**:

Поле `model` должно быть:
```
model: "gpt-5.1"  (было "gpt-5")
```

---

## 🎯 Ожидаемый результат

### Для пользователя 5857876 (вы):

**До миграции** (сейчас):
```json
{
  "user_id": 5857876,
  "language": "ru",
  "reasoning": "low",
  "model": "gpt-5"  ← СТАРОЕ
}
```

**После миграции**:
```json
{
  "user_id": 5857876,
  "language": "ru", 
  "reasoning": "low",
  "model": "gpt-5.1"  ← НОВОЕ!
}
```

### Для новых пользователей:

Автоматически получают:
```json
{
  "model": "gpt-5.1-mini"  ← Дефолт из кода
}
```

---

## ⏱️ Временная шкала

### Прямо сейчас:

1. ⏳ Koyeb пересобирается с последним кодом (~5 мин)
2. ⏳ Деплоит новую версию (~2 мин)
3. ✅ Instance становится Healthy

### Через 10 минут:

1. ➕ Добавляете `RUN_MODEL_MIGRATION=true`
2. ⏳ Koyeb автоматически редеплоится (~2 мин)
3. ✅ Миграция запускается
4. ✅ В логах: "Model names migration completed"

### Через 15 минут:

1. 🗑️ Удаляете `RUN_MODEL_MIGRATION` переменную
2. ✅ Готово!

---

## 🧪 Тестирование после миграции

### Отправьте новую геолокацию

В логах должно быть:
```
✅ GPT-5.1 Responses: sending request (model=gpt-5.1, reasoning=low, tool_choice=auto)
                                              ^^^^^^^^ НЕ "gpt-5"!
✅ GPT-5.1 Responses: success (web_search enabled)
```

**НЕ должно быть**:
```
❌ model=gpt-5  (старое название)
```

---

## 🐛 Если что-то пойдёт не так

### Ошибка: "Model migration failed"

**Скопируйте полную ошибку из логов** и отправьте мне.

Возможные проблемы:
- Firestore credentials неправильные
- Permissions недостаточно
- Скрипт не импортируется

### Миграция не запустилась

Проверьте что:
1. ✅ Переменная `RUN_MODEL_MIGRATION=true` добавлена
2. ✅ Service задеплоился после добавления
3. ✅ В логах есть строки про "Starting Bot Voyage"

### Миграция запустилась, но "0 users updated"

Это **нормально** если:
- Новые пользователи (уже с gpt-5.1-mini)
- Или все уже мигрированы ранее

---

## ✅ Чек-лист миграции

- [ ] Текущий deployment стал Healthy (~10 мин от сейчас)
- [ ] Добавлена переменная `RUN_MODEL_MIGRATION=true`
- [ ] Подождали нового deployment (~2 мин)
- [ ] В логах: "✅ Model names migration completed"
- [ ] Удалена переменная `RUN_MODEL_MIGRATION`
- [ ] Протестировали: логи показывают `model=gpt-5.1`

---

## 🚀 Начинайте!

**Сейчас**: Подождите 10 минут (пока Koyeb пересоберётся)

**Через 10 минут**: Добавьте `RUN_MODEL_MIGRATION=true` и следуйте инструкциям выше!

Дайте знать когда увидите "Model names migration completed" в логах! 🎉

